"""
AxiomGuard Integrations — Vector DB wrappers for RAG pipelines.

Available integrations (install with optional deps):
  pip install axiomguard[chroma]   → VerifiedCollection
  pip install axiomguard[qdrant]   → VerifiedQdrant
  pip install axiomguard[all]      → everything
"""
