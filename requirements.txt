# Core
z3-solver>=4.12.0   # formal contradiction engine (required)

# LLM backends (pick the one you need — or use the mock with zero extra deps)
anthropic>=0.39.0   # for axiomguard.backends.anthropic_llm
openai>=1.0.0       # for axiomguard.backends.openai_llm
# httpx is already installed with anthropic/openai — used by generic_http_llm
